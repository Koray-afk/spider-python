{
  "discover": "() => { function visible(el) { const r = el.getBoundingClientRect(); const s = window.getComputedStyle(el); return r.width > 0 && r.height > 0 && s.visibility !== 'hidden' && s.display !== 'none'; } function label(el) { return (el.innerText || el.value || el.getAttribute('aria-label') || el.getAttribute('title') || '').trim().slice(0, 120); } document.querySelectorAll('[data-crawl-id]').forEach(el => el.removeAttribute('data-crawl-id')); const specs = ['button', '[aria-haspopup]', '[role=\"button\"]', '[role=\"menuitem\"]', '[role=\"tab\"]', 'input[type=\"submit\"]', 'input[type=\"button\"]']; const out = [], seen = new Set(); let n = 0; for (const sel of specs) { for (const el of document.querySelectorAll(sel)) { if (el.tagName === 'A') continue; if (!visible(el)) continue; const key = el.tagName + '|' + (el.className || '') + '|' + label(el); if (seen.has(key)) continue; seen.add(key); n += 1; el.setAttribute('data-crawl-id', String(n)); out.push({ label: label(el) || el.tagName.toLowerCase(), id: el.id || '', className: typeof el.className === 'string' ? el.className : '', selector: '[data-crawl-id=\"' + n + '\"]', elementType: el.tagName.toLowerCase() }); } } return out; }",
  "classify": "() => { function vis(sel) { for (const el of document.querySelectorAll(sel)) { const r = el.getBoundingClientRect(); const s = window.getComputedStyle(el); if (r.width > 0 && r.height > 0 && s.display !== 'none' && s.visibility !== 'hidden') return true; } return false; } if (vis('.modal.show, .modal.in, [role=\"dialog\"]')) return 'modal'; if (vis('.offcanvas.show, .drawer.open, .drawer.show')) return 'drawer'; if (vis('.sidebar.open, .side-panel.open, aside.sidebar-panel')) return 'sidebar'; if (vis('.dropdown-menu.show, [role=\"menu\"], [role=\"listbox\"], .dropdown-menu')) return 'dropdown'; if (vis('.popover.show, .popover.in, [role=\"tooltip\"][data-popover], .popover')) return 'popover'; if (vis('.tooltip.show, .tooltip.in, [role=\"tooltip\"]')) return 'tooltip'; if (vis('.overlay.show, .modal-backdrop.show, .cdk-overlay-pane')) return 'overlay'; return 'unknown'; }",
  "fingerprint": "() => ({ len: document.body ? document.body.innerHTML.length : 0, overlays: document.querySelectorAll('.modal, [role=\"dialog\"], .dropdown-menu, .drawer, .offcanvas').length })",
  "extract_element": "(el) => { function cssSel(e) { if (e.id) return '#' + CSS.escape(e.id); const parts = []; let cur = e; while (cur && cur.nodeType === 1 && cur.tagName.toLowerCase() !== 'html') { if (cur.id) { parts.unshift('#' + CSS.escape(cur.id)); break; } let nth = 1, sib = cur; while (sib.previousElementSibling) { sib = sib.previousElementSibling; if (sib.tagName === cur.tagName) nth++; } parts.unshift(cur.tagName.toLowerCase() + ':nth-of-type(' + nth + ')'); cur = cur.parentElement; } return parts.join(' > '); } function xp(e) { if (e.id) return '//*[@id=' + JSON.stringify(e.id) + ']'; const parts = []; let cur = e; while (cur && cur.nodeType === 1) { let idx = 1, sib = cur.previousElementSibling; while (sib) { if (sib.tagName === cur.tagName) idx++; sib = sib.previousElementSibling; } parts.unshift(cur.tagName.toLowerCase() + '[' + idx + ']'); cur = cur.parentElement; } return '/' + parts.join('/'); } const attr = (n) => el.getAttribute(n) || ''; return { tag_name: el.tagName ? el.tagName.toLowerCase() : '', text: (el.innerText || '').trim().slice(0, 300), id: el.id || '', class_name: typeof el.className === 'string' ? el.className : '', name: attr('name'), role: attr('role'), href: attr('href'), src: attr('src'), data_testid: attr('data-testid'), aria_label: attr('aria-label'), css_selector: cssSel(el), xpath: xp(el), outer_html: el.outerHTML || '' }; }",
  "active_tabs": "() => { const sels = ['[role=\"tab\"]', '.nav-link', '.tab-item', '[data-toggle=\"tab\"]', '[data-bs-toggle=\"tab\"]']; const out = [], seen = new Set(); for (const sel of sels) { for (const el of document.querySelectorAll(sel)) { const active = el.classList.contains('active') || el.classList.contains('selected') || el.getAttribute('aria-selected') === 'true' || el.getAttribute('aria-current') === 'page'; if (!active) continue; const key = el.id || (el.innerText || '').trim().slice(0, 40); if (seen.has(key)) continue; seen.add(key); out.push({ id: el.id || '', text: (el.innerText || '').trim().slice(0, 80), className: typeof el.className === 'string' ? el.className : '' }); } } return out; }",
  "active_tab_panel": "() => { function cssSel(el) { if (el.id) return '#' + CSS.escape(el.id); let nth = 1, sib = el; while (sib.previousElementSibling) { sib = sib.previousElementSibling; if (sib.tagName === el.tagName) nth++; } const p = el.parentElement; const ps = p && p.id ? '#' + CSS.escape(p.id) : (p ? p.tagName.toLowerCase() : ''); return ps + ' > ' + el.tagName.toLowerCase() + ':nth-of-type(' + nth + ')'; } const sels = ['[role=\"tabpanel\"]:not([hidden])', '.tab-pane.active.show', '.tab-pane.active', '.tab-content > .active']; for (const sel of sels) { for (const el of document.querySelectorAll(sel)) { const r = el.getBoundingClientRect(); const s = window.getComputedStyle(el); if (r.width > 0 && r.height > 0 && s.display !== 'none' && s.visibility !== 'hidden') { return { selector: cssSel(el), outerHTML: el.outerHTML }; } } } return null; }",
  "sidebar_links": "() => { const root = document.querySelector('#main-nav-tab') || document.querySelector('.main-nav-lhs'); if (!root) return []; const out = [], seen = new Set(); for (const a of root.querySelectorAll('a[href]')) { const href = (a.getAttribute('href') || '').trim(); if (!href || href.startsWith('javascript:')) continue; if (a.classList.contains('add-new')) continue; const label = (a.innerText || a.getAttribute('title') || a.getAttribute('aria-label') || '').trim().replace(/\\s+/g, ' ').slice(0, 80); const key = href + '|' + label; if (seen.has(key)) continue; seen.add(key); out.push({ href, label }); } return out; }"
}
